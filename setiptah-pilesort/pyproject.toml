[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "setiptah-pilesort"
dynamic = ["version"]
authors = [
    { name = "Kyle Treleaven", email = "ktreleav@gmail.com" }
]
readme = "README.md"
dependencies = [
    "numpy",
]

[project.optional-dependencies]
dev = [
    "matplotlib",
    "ipython",
    "notebook",
    "ipywidgets",
    "pandas",
    "nbconvert",
]
test = [
    "pytest",
    "pytest-coverage",
]
package = [
    "black",
    "sphinx",
    "sphinx_rtd_theme",
    "sphinx-codeautolink",
    "sphinx-autobuild",
    "nbsphinx",
]
